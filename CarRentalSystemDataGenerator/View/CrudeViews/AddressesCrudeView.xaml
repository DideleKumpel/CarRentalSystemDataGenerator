<UserControl x:Class="CarRentalSystemDataGenerator.View.CrudeViews.AddressesCrudeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CarRentalSystemDataGenerator.View.CrudeViews"
             xmlns:crudevm="clr-namespace:CarRentalSystemDataGenerator.ViewModel.CrudeViewModels"
             d:DataContext="{d:DesignInstance Type=crudevm:AddressesCrudeViewModel, IsDesignTimeCreatable=True}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="auto"/>
        </Grid.ColumnDefinitions>
        <StackPanel Orientation="Horizontal" Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2">
            <StackPanel Orientation="Vertical" Margin="5">
                <Label Content="ID"/>
                <TextBox Width="40" Margin="5"
                     Text="{Binding Id}" IsReadOnly="True"/>
            </StackPanel>
            <StackPanel Orientation="Vertical" Margin="5">
                <Label Content="City"/>
                <TextBox Width="150" Margin="5"
                     Text="{Binding City}"/>
            </StackPanel>
            <StackPanel Orientation="Vertical" Margin="5">
                <Label Content="Street"/>
                <TextBox Width="150" Margin="5"
                     Text="{Binding Street}"/>
            </StackPanel>
            <StackPanel Orientation="Vertical" Margin="5">
                <Label Content="House number"/>
                <TextBox Width="150" Margin="5"
                     Text="{Binding HouseNum}"/>
            </StackPanel>
            <StackPanel Orientation="Vertical" Margin="5">
                <Label Content="Postal code"/>
                <TextBox Width="150" Margin="5"
                     Text="{Binding PostalCode}"/>
            </StackPanel>
        </StackPanel>

        <StackPanel Orientation="Vertical" Grid.Row="1" Grid.Column="1">
            <Button Content="Add Customer" Margin="5" Command="{Binding AddAddressCommand}"/>
            <Button Content="Edit" Margin="5" Command="{Binding UpdateAddressCommand}"/>
            <Button Content="Delete" Margin="5" Command="{Binding DeleteAddressCommand}"/>
            <Button Content="Refresh" Margin="5" Command="{Binding LoadAddressesCommand}"/>
        </StackPanel>

        <DataGrid Grid.Row="1" Grid.Column="0"
              ItemsSource="{Binding AddressesList}"
              SelectedItem="{Binding SelectedAddress}"
              SelectionUnit="FullRow"
              CanUserAddRows="False" CanUserDeleteRows="False" IsReadOnly="True"
              AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Id" Binding="{Binding AddressID}" Width="2*"/>
                <DataGridTextColumn Header="City" Binding="{Binding City}" Width="7*"/>
                <DataGridTextColumn Header="Street" Binding="{Binding Street}" Width="7*"/>
                <DataGridTextColumn Header="House number" Binding="{Binding HouseNumber}" Width="7*"/>
                <DataGridTextColumn Header="Postal code" Binding="{Binding PostalCode}" Width="5*"/>
            </DataGrid.Columns>

        </DataGrid>
    </Grid>
</UserControl>
